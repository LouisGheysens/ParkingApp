import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ParkingMapScreen = () => {
  return (
    <View>
      <Text>ParkingMapScreen</Text>
    </View>
  );
};

export default ParkingMapScreen;

const styles = StyleSheet.create({});
